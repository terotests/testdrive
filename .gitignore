# The compiler's output. Everything here is regenerated by `npm run build`.
build/

# Dependencies fetched by `rgrc install -vendor` (see scripts/add-gallery.sh).
# These are other projects' sources under their own licenses — the gallery is
# AGPL-3.0-or-later. Do not commit them into an MIT tree by accident.
vendor/

node_modules/
npm-debug.log*
.DS_Store
bin/StarterMain.js

# Android Gradle host (generated Kotlin under app/src/main/generated/)
platforms/android/.gradle/
platforms/android/**/build/
platforms/android/local.properties
platforms/android/app/src/main/generated/
